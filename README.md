# CRM API Automation POC

Automated testing framework for the backend of the React Admin-based CRM.

## Technologies
- Java + Maven
- REST-assured + TestNG
- PostgreSQL (JDBC)
- Twilio (sandbox)
- Allure Reports

## Run tests
```bash
  mvn clean test
  mvn allure:serve
```
